import { Link } from 'react-router-dom';
import { categories } from '../data/products';
export default function Collections(){return <><section className="page-hero"><p className="eyebrow">Curated worlds</p><h1>Our collections</h1><p>Elegant heritage and contemporary luxury for every occasion.</p></section><section className="collection-list section-pad">{categories.map((item,index)=><Link key={item.id} to={`/shop?category=${item.id}`} className={`collection-row ${index%2?'reverse':''}`}><div className={`collection-image sheet ${item.image}`}></div><div><span>{String(index+1).padStart(2,'0')} · {item.label}</span><h2>{item.title}</h2><p>{item.copy}</p><em>Explore collection →</em></div></Link>)}</section></>}
