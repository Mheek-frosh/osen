import { useEffect, useMemo, useState } from 'react';
import { useSearchParams } from 'react-router-dom';
import ProductGrid from '../components/ProductGrid';
import { useCatalog } from '../context/CatalogContext';
export default function Search(){const [params,setParams]=useSearchParams();const [query,setQuery]=useState(params.get('q')||'');const {products}=useCatalog();useEffect(()=>setParams(query?{q:query}: {},{replace:true}),[query,setParams]);const items=useMemo(()=>query?products.filter(p=>`${p.name} ${p.material} ${p.label}`.toLowerCase().includes(query.toLowerCase())):products,[query,products]);return <section className="search-page section-pad"><p className="eyebrow">Find your piece</p><div className="search-form"><input value={query} onChange={e=>setQuery(e.target.value)} type="search" placeholder="Search dresses, bags, shoes…" autoFocus/><span>Search</span></div><p className="search-status">{query?`${items.length} result${items.length===1?'':'s'} for “${query}”`:'Browse all pieces'}</p><ProductGrid items={items}/></section>}
