VITE_SANITY_PROJECT_ID=your-project-id
VITE_SANITY_DATASET=production

# Used only by the one-time terminal import. Never expose this as a VITE_ variable.
SANITY_WRITE_TOKEN=your-temporary-editor-token
